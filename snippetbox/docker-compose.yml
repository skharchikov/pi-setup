version: '3'
services:
  snippet-box:
    image: pawelmalak/snippet-box:latest
    container_name: snippet-box
    volumes:
      - ./app/data:/app/data
    ports:
      - 5000:5000
    restart: unless-stopped
