# Template for homeassistant/config/secrets.yaml
# Copy to config/secrets.yaml and fill in real values. Never commit secrets.yaml.
# Reference from other YAML via:  password: !secret some_password

some_password: changeme
